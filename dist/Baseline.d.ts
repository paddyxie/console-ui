/** Injects Google Fonts <link> into <head> exactly once. */
export declare function FontLoader(): null;
/** Global reset + scrollbar styles (uses --ui-* css vars for light/dark). */
export declare function BaselineStyles(): import("react/jsx-runtime").JSX.Element;
